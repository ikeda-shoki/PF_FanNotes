<% unless user === current_user %>
  <% if current_user.following?(user) %>
    <%= link_to "フォローを外す", unfollow_path(user), method: :delete, class: "btn btn-danger" %>
  <% else %>
    <%= link_to "フォローする", follow_path(user), method: :post, class: "btn btn-success" %>
  <% end %>
<% end %>